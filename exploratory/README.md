# Exploratory scripts

These files document earlier stages of development and are retained for transparency.
They are not the source of the dissertation's final strict results.

In particular, `baseline_RoBERTa.py` evaluates checkpoints on the nominal test split
during training and must not be used to reproduce the final reported RoBERTa result.
Use `strict_roberta_training.py` in the repository root instead.
